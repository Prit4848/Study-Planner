<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Study Planner</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
  <header class="bg-blue-600 p-4 text-white shadow-md">
      <div class="container mx-auto flex justify-between items-center">
          <h1 class="text-2xl font-bold">
              <a href="/" class="hover:text-gray-300 transition duration-300">Study Planner</a>
          </h1>
          <nav class="space-x-4">
              <% if (user) { %>
                  <a href="/dashboard" class="bg-green-500 text-white px-4 py-2 rounded-md hover:bg-green-600 transition duration-300 ease-in-out">Dashboard</a>
                  <a href="/user/logout" class="bg-red-500 text-white px-4 py-2 rounded-md hover:bg-green-600 transition duration-300 ease-in-out">Logout</a>
              <% } else { %>
                  <a href="/user/login" class="bg-blue-500 text-white px-4 py-2 rounded-md hover:bg-green-600 transition duration-300 ease-in-out">Login</a>
                  <a href="/user/register" class="bg-green-500 text-white px-4 py-2 rounded-md hover:bg-green-600 transition duration-300 ease-in-out">Register</a>
              <% } %>
          </nav>
      </div>
  </header>